{
  "name": "m1-s1-2025-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd react-app && npm run dev",
    "build": "cd react-app && npm run build",
    "lint": "cd react-app && npm run lint",
    "lint:fix": "cd react-app && npm run lint:fix",
    "format": "cd react-app && npm run format",
    "format:check": "cd react-app && npm run format:check",
    "preview": "cd react-app && npm run preview"
  }
}

